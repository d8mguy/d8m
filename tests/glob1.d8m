// Run the Glob function in io/fs pkg, also using os.DirFS
import go "io/fs"
import go "os"
val foo = \imp() {
    val rslt = fs.Glob(os.DirFS("/Users/curtisabbott/go/src/github.com/curtisabbott/d8mingo/tests"), "bintree.*")
    if(rslt.err != nil) exit(rslt.err)
    rslt.ok
}
foo()
